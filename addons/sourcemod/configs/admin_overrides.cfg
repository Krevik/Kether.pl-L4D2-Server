Overrides
{
	/**
	 * By default, commands are registered with three pieces of information:
	 * 1)Command Name 		(for example, "csdm_enable")
	 * 2)Command Group Name	(for example, "CSDM")
	 * 3)Command Level		(for example, "changemap")
	 *
	 * You can override the default flags assigned to individual commands or command groups in this way.
	 * To override a group, use the "@" character before the name.  Example:
	 * Examples:
	 *		"@CSDM"			"b"				// Override the CSDM group to 'b' flag
	 * 		"csdm_enable"	"bgi"			// Override the csdm_enable command to 'bgi' flags
	 *
	 * Note that for overrides, order is important.  In the above example, csdm_enable overwrites
	 * any setting that csdm_enable previously had.
	 *
	 * You can make a command completely public by using an empty flag string.
	 */
	 
	"sm_gag"        "o"        // Default = j    // Change "sm_gag" to the "p" (Custom2) flag.
    "sm_ungag"        "o"        // Default = j    // Change "sm_ungag" to the "p" (Custom2) flag.
	"sm_hatshowoff"        "p"        // Default = j    // Change "sm_ungag" to the "p" (Custom2) flag.
	"sm_hatshowon"        "p"        // Default = j    // Change "sm_ungag" to the "p" (Custom2) flag.
	"sm_hatview"        "p"        // Default = j    // Change "sm_ungag" to the "p" (Custom2) flag.
	"sm_hatshow"        "p"        // Default = j    // Change "sm_ungag" to the "p" (Custom2) flag.
	"sm_hatoff"        "p"        // Default = j    // Change "sm_ungag" to the "p" (Custom2) flag.
	"sm_hat"        "p"        // Default = j    // Change "sm_ungag" to the "p" (Custom2) flag.
	"sm_wskin"        "q"        // Default = j    // Change "sm_ungag" to the "p" (Custom2) flag.
	"sm_voice"        "r"        // Default = j    // Change "sm_ungag" to the "p" (Custom2) flag.
	"sm_voicedisable"        "r"        // Default = j    // Change "sm_ungag" to the "p" (Custom2) flag.
	"sm_voicereset"        "r"        // Default = j    // Change "sm_ungag" to the "p" (Custom2) flag.
	"sm_give"        "t"        // Default = j    // Change "sm_ungag" to the "p" (Custom2) flag.
	"sm_fs"        "s"        // Default = j    // Change "sm_ungag" to the "p" (Custom2) flag.
	"sm_forcestart"        "s"        // Default = j    // Change "sm_ungag" to the "p" (Custom2) flag.

}

